package com.example.petio.Models;

public class ChatList {
    public String userName, lastMessage;

    public ChatList(String userName, String lastMessage) {
        this.userName = userName;
        this.lastMessage = lastMessage;
    }
}
